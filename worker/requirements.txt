# Keep the worker image small: only the Supabase REST repository and configuration
# dependencies are needed by the launch handlers.
httpx==0.28.1
requests==2.34.2
pydantic==2.11.9
fastapi==0.141.1
starlette==1.6.0
cryptography==50.0.0
playwright==1.49.1
